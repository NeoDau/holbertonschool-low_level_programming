#include "main.h"
/**
 *_strcmp - function two strings
 *@s1: char
 *@s2: char
 *Return: s1[a] & s2[a]
 */

int _strcmp(char *s1, char *s2)
{
	int a;

	a = 0;

	for (s1[a] - s2[a] == 0 && s1[a] != '\0'; a++)
	{}
	return (s1[a] - s2[a]);
}
